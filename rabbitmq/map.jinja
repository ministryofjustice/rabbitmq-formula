{% set rabbitmq = salt['grains.filter_by']({
    'Debian': {
      'cluster_hosts': None,
      'erlang': {
        'cookie': ''
      }
    },
'default': 'Debian',
}, merge=salt['pillar.get']('rabbitmq',{})) %}
